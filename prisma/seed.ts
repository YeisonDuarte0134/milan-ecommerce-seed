// Seed script — no longer needed since products come from PostgreSQL (Odoo).
// Kept for reference. User and CartItem tables are auto-created by Prisma migrate.

async function main() {
  console.log("No seed needed — products come from PostgreSQL via MCP.");
  console.log("User and CartItem tables are managed by Prisma migrations.");
}

main();
